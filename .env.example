# Copy this file to `.env.local` and fill with real values. Do NOT commit your real keys.

# Supabase (required)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your_anon_key...
# (Optional for imports only) Service Role Key - keep secret; do NOT commit
# SUPABASE_SERVICE_ROLE_KEY=eyJ...your_service_role_key...

# Optional: Gemini / Google AI keys used by your app
GEMINI_API_KEY=PLACEHOLDER_API_KEY

# Notes:
# - The project uses Vite; env vars must start with VITE_ to be exposed to the client.
# - For local development, copy to `.env.local` or `.env` and keep that file out of version control.
